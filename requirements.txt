# Доступ к Firebird (локальная копия .GDB через FB 2.5 embedded)
fdb==2.0.4

# Данные и аналитика
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0

# Локальное хранилище (PostgreSQL цель, SQLite fallback)
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# Конфигурация
python-dotenv==1.0.1

# Инкрементальные обновления через Proxy API
requests==2.32.3

# Веб-интерфейс
Flask==3.1.0

# Тесты
pytest==8.3.4
